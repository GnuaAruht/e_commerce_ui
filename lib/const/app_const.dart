import 'package:flutter/material.dart';

const defaultPadding = 18.0;
const defaultBorderRadius = 18.0;
const appBarIconSize = 24.0;

const defaultBoxShadow = BoxShadow(
  offset: Offset(2, 4),
  blurRadius: 12,
  color: Colors.black26,
);
